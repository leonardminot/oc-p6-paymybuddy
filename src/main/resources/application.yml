spring:
  datasource:
    username: postgres
    password: ''
    url: jdbc:postgresql://localhost:5432/paymybuddy
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true